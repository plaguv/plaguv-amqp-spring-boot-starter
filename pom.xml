<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.3</version>
    </parent>

    <groupId>io.github.plaguv</groupId>
    <artifactId>plaguv-amqp-parent</artifactId>
    <version>1.2.0</version>
    <packaging>pom</packaging>

    <modules>
        <module>plaguv-amqp-contract</module>
        <module>plaguv-amqp-core</module>
        <module>plaguv-amqp-spring-boot-starter</module>
    </modules>

    <name>Plaguv AMQP Parent</name>
    <description>
        Parent POM for the Plaguv AMQP project.
        Provides dependency management, plugin configuration, and module aggregation.
    </description>

    <licenses>
        <license>
            <name>${license.name}</name>
            <url>${license.url}</url>
        </license>
    </licenses>

    <scm>
        <connection>${scm.connection}</connection>
        <developerConnection>${scm.developerConnection}</developerConnection>
        <url>${scm.url}</url>
    </scm>

    <developers>
        <developer>
            <id>${developer.name}</id>
            <name>${developer.name}</name>
            <url>${developer.url}</url>
        </developer>
    </developers>

    <properties>
        <!-- Meta -->
        <license.name>Apache License 2.0</license.name>
        <license.url>https://www.apache.org/licenses/LICENSE-2.0.txt</license.url>
        <scm.connection>scm:git:https://github.com/plaguv/plaguv-amqp-spring-boot-starter.git</scm.connection>
        <scm.developerConnection>scm:git:ssh://github.com/plaguv/plaguv-amqp-spring-boot-starter.git</scm.developerConnection>
        <scm.url>https://github.com/plaguv/plaguv-amqp-spring-boot-starter</scm.url>
        <developer.name>plaguv</developer.name>
        <developer.url>https://github.com/plaguv</developer.url>
        <!-- Application -->
        <plaguv.amqp.api.version>1.2.0</plaguv.amqp.api.version>
        <plaguv.amqp.core.version>1.2.0</plaguv.amqp.core.version>
        <plaguv.amqp.starter.version>1.2.0</plaguv.amqp.starter.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.github.plaguv</groupId>
                <artifactId>plaguv-amqp-api</artifactId>
                <version>${plaguv.amqp.api.version}</version>
            </dependency>
            <dependency>
                <groupId>io.github.plaguv</groupId>
                <artifactId>plaguv-amqp-core</artifactId>
                <version>${plaguv.amqp.api.version}</version>
            </dependency>
            <dependency>
                <groupId>io.github.plaguv</groupId>
                <artifactId>plaguv-amqp-starter</artifactId>
                <version>${plaguv.amqp.api.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>